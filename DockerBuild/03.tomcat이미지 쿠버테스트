######################
# 오류노트
######################

1. Neither ther JAVA_HOME nor the JRE_HOME environment variable is defined
 -> /root/.bashrc에서 환경변수 등록했기 때문
 -> /root/.bash_profile에 등록해서 OK
 
 

vi /root/default-mytomcat9openjava8-deployment.yaml
----------------------------------------------------------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: default-mytomcat9openjava8-deployment
  namespace: default
  labels:
    app: default-mytomcat9openjava8
    tier: backend
    role: was
    deployment: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: default-mytomcat9openjava8
      tier: backend
      role: was
      deployment: v1
  template:
    metadata:
      labels:
        app: default-mytomcat9openjava8
        tier: backend
        role: was
        deployment: v1
    spec:
      containers:
      - name: default-mytomcat9openjava8-deployment-pod
        image: reg.hmp.com/mytomcat9openjava8:latest
        ports:
        - name: port8009
          containerPort: 8009
        - name: port8080
          containerPort: 8080
        resources:
          limits:
            memory: "2Gi"
          requests:
            memory: "1Gi"
        command: ["sh", "-c", "/app/tomcat9/bin/startup.sh"]
----------------------------------------------------------------------------------------------------------------------

